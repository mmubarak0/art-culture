$(function () {
    // Set the default AJAX headers.
    $.ajaxSetup({
        headers: {
            "Content-Type": "application/json",
            Accept: "application/json",
        },
    });
});
